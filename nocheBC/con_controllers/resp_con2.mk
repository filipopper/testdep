//Verdadero o falso:
//*)PHP soporta la sobrecarga (característica de la Programación orientada a objetos).
// Falso

//*)Si quiero enviar un archivo mediante formulario debo cambiar el atributo enctype de un formulario al valor multipart/form-data
// Verdadero, para enviar archivos mediante formulario se debe cambiar el atributo enctype de un formulario al valor multipart/form-data y el tipo de input debe ser file.

//*)La herencia no permite realizar polimorfismo. Este solamente se realiza por implementación de interfaces.
// Falso, la herencia permite realizar polimorfismo, este solamente se realiza por implementación de interfaces.

//*)La palabra directiva inlcude_once sirve para incluir otro fichero en php pero si el archivo no se encuentra en el path especificado, la ejecución del programa se interrumpe.
// Falso, la palabra directiva include_once sirve para incluir otro fichero en php pero si el archivo no se encuentra en el path especificado, la ejecución del programa no se interrumpe.

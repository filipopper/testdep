//*)Las cookies son archivos que se almacenan en el PC del usuario que visita la web.
// Verdadero, las cookies son archivos que se almacenan en el PC del usuario que visita la web.

//*)Para usar una sesión en php se debe llamar al método session_start()
// Verdadero, para usar una sesión en php se debe llamar al método session_start()

//*)¿Qué pasa si escribo una url en el navegador?
// El navegador consulta el servidor DNS para obtener la dirección IP del servidor web, luego se conecta al servidor web y le envía la petición HTTP, el servidor web procesa la petición y devuelve la respuesta HTTP al navegador.

//*)¿Para qué sirven los parámetros opcionales en PHP?
// Para que una función pueda recibir un número variable de argumentos.
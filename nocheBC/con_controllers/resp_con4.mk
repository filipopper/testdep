//*)Cuál es la diferencia entre require_once e include_once.
// La diferencia es que require_once detiene la ejecución del programa si el archivo no se encuentra, mientras que include_once no detiene la ejecución del programa si el archivo no se encuentra.

//*)Explique que es la herencia.
// La herencia es una característica de la Programación orientada a objetos que permite que una clase herede los atributos y métodos de otra clase.
// ejemplo con galletitas: si tenemos una clase galleta, podemos crear una clase galletaSalada que herede los atributos y métodos de la clase galleta, y agregarle nuevos atributos y métodos.

//*)Para qué sirve la clase PDO.
// Para conectarse a una base de datos.

//*)Explique que es el sql injection, que podemos hacer para manejarlo.
// Es una vulnerabilidad de seguridad que permite ejecutar código SQL desde un formulario web. Para manejarlo se puede utilizar la función mysqli_real_escape_string().
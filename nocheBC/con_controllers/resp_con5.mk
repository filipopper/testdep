//*)Para qué sirven los modificadores de acceso.
// Para controlar el acceso a los atributos y métodos de una clase.

//*)Para realizar el login que tipo de petición usaría, explique?
// Usaría una petición POST, ya que los datos del formulario se envían al servidor de forma oculta y no se muestran en la URL.

//*)Para qué sirven las sesiones como accedo a ellas.
// Las sesiones en PHP son objetos que se utilizan para almacenar información temporalmente mientras el usuario navega por un sitio web.

//*)¿Qué pasa si abrimos un archivo index.php con el navegador?
// El navegador web no puede interpretar el código de estos archivos, por lo que no se mostrará nada, podría mostrar un error o simplemente el código sin interpretarlo.